/** Scoreboard — PLACEHOLDER. Being rewritten in the TypeScript + Tailwind redesign.
 *  The old implementation is at src/screens/Scoreboard.jsx and is the behavioural
 *  reference: every API call and every field it reads must survive the port. */
export default function Scoreboard() {
  return (
    <div className="rounded-lg border border-border bg-surface p-10 text-center text-sm text-dim">
      Scoreboard — not yet ported
    </div>
  )
}
